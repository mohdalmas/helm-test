{{- if .Values.rbac.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "rosita-chatbot.fullname" . }}
  labels:
    {{- include "rosita-chatbot.labels" . | nindent 4 }}
  annotations:
    {{- toYaml .Values.rbac.annotations  | nindent 4 }}

{{- end }}
